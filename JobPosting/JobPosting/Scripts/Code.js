$(document).ready(function () {
    $(".custom-select2").select2()

    
    //$("#submit").click(function () {
    //    $(".listBox option").prop("selected", true)
    //});
    //$("#addDay").click(function () {
    //    var dayIDs = [];
    //    $("#dayListBox option").each(function () {
    //        dayIDs.push($(this).val());
    //    });
    //    var itemToAdd = $("#dayID :selected");
    //    var existed = check(dayIDs, itemToAdd.val());
    //    if (!existed) {
    //        $("#dayListBox").append($("<option></option>").attr("value", itemToAdd.val()).text(itemToAdd.text()));
    //    } else {
    //        console.log("Unable to add"); // need change
    //    }

    //});
    //$("#removeDay").click(function () {
    //    $("#dayListBox option:selected").remove();
    //});

    //$("#addLocation").click(function () {
    //    var locationIDs = [];
    //    $("#locationListBox option").each(function () {
    //        locationIDs.push($(this).val());
    //    });
    //    var itemToAdd = $("#jobLocationID :selected");
    //    var existed = check(locationIDs, itemToAdd.val());
    //    if (!existed) {
    //        $("#locationListBox").append($("<option></option>").attr("value", itemToAdd.val()).text(itemToAdd.text()));
    //    } else {
    //        console.log("Unable to add"); // need change
    //    }

    //});
    //$("#removeLocation").click(function () {
    //    $("#locationListBox option:selected").remove();
    //});

    //$("#addRequirement").click(function () {
    //    var qualificationIDs = [];
    //    $("#qualificationListBox option").each(function () {
    //        qualificationIDs.push($(this).val());
    //    });
    //    var itemToAdd = $("#jobRequirementID :selected");
    //    var existed = check(qualificationIDs, itemToAdd.val());
    //    if (!existed) {
    //        $("#qualificationListBox").append($("<option></option>").attr("value", itemToAdd.val()).text(itemToAdd.text()));
    //    }
    //    else {
    //        console.log("Unable to add"); // need change
    //    }
    //});
    //$("#removeRequirement").click(function () {
    //    $("#qualificationListBox option:selected").remove();
    //})

    //$("#addSkill").click(function () {
    //    var skillIDs = [];
    //    $("#skillListBox option").each(function () {
    //        skillIDs.push($(this).val());
    //    });
    //    var itemToAdd = $("#postingSkillID :selected");
    //    var existed = check(skillIDs, itemToAdd.val());
    //    if (!existed) {
    //        $("#skillListBox").append($("<option></option>").attr("value", itemToAdd.val()).text(itemToAdd.text()));
    //    }
    //    else {
    //        console.log("Unable to add"); // need change
    //    }
    //});
    //$("#removeSkill").click(function () {
    //    $("#skillListBox option:selected").remove();
    //});
    
})